// This variable stores the student's name.
// The type is explicitly defined as string.

let studentName: string = "shayon Boidya";

console.log(studentName);


// This variable stores whether the student is active.
// Boolean can only contain true or false.

let isActive: boolean = true;

console.log(isActive);