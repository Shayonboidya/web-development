const student = {
    name : "shayon boidya",
    age : 21,
    gread : 3.733,
    isEnrolled: true
}
// consol log
console.log(student);
// consol log name
console.log(student.name);

// An object is like a backpack. It stores different things (its data, like books,
// pencils, and a water bottle) and also has actions it can perform, like opening,
// closing, or carrying items. In programming, an object groups related data and
// behaviors together in one place.